# kabu-app

kabu の本番コード。取得バッチ、DB スキーマ、XBRL パーサ。ラズパイが pull して実行する。

## ドメイン知識はここに書かない

株式・会計・銘柄の知識は `../kabu-vault` にある。`/query` で引き、一般化できる知見は `/session` で戻す。このリポジトリで完結させないこと。

境界の判断軸は 1 本。

> このアプリを捨てて別のアプリを作り直したとき、その記述は生き残るか。

生き残るなら vault、一緒に無価値になるならここ。「EDINET API は直近 5 年分しか遡れない」は vault、「銘柄テーブルは証券コードを主キーにする」はここ。

vault からこのリポジトリの実装ファイルへリンクは張らない。リンクは**コード → wiki の一方向**に限る。

## 実装の約束

- データのパスをコードに埋めない。`KABU_DATA_DIR` で受ける (Mac は `kabu-terminal/data`、ラズパイは `/mnt/usb/data`)
- Mac 側の `data` は SMB の読み取り専用マウント。書き込む処理を Mac で動かさない
- バッチは systemd timer で回し、冪等に作る。同じ日に 2 回走っても壊れないこと
- PostgreSQL への接続はロールを使い分ける。バッチは `kabu_app` (localhost のみ)、開発は `kabu_dev`
